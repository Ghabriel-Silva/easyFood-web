export interface EditeUserInfo {
    message: string,
    cache: null,
    data: {
        name: string,
        defaultFreight: string,
        customerAddress: string,
        customerPhone: string,
    }
}
